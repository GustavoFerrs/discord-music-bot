version: '3.8'

services:
  discord-bot:
    build: .
    container_name: discord-bot
    restart: always
    env_file: .env
    volumes:
      - ./music:/app/music
    logging:
      options:
        max-size: "10m"
        max-file: "3"
